const StatusFilter = () => {
  return (
    <div id="filter-group">
      <button>All</button>
      <button>Completed</button>
      <button>Pending</button>
    </div>
  );
};

export default StatusFilter;
